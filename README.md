# MQTT WEBHOOK
A bridge for communication between API server and ESP32.

| This is a temporary fix because vercel can't handle mqtt connections, If we have a paid hosting like hostinger in the future we will integrate mqtt directly to the server.